import 'package:flutter/material.dart';

class Condominum extends StatelessWidget {
  const Condominum({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SliverToBoxAdapter(
      child: Text("View Condominum"),
    );
  }
}
